@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Interface View for Leave Request BO'
define root view entity Z_I_LeaveReq 
       as select from zrev_lv_req
                                  
{
  key request_id,
      emp_id,
      leave_type,
      date_from,
      date_to,
      leave_days,
      status,
      reason,
      created_at,
      created_by,
      @Semantics.systemDateTime.lastChangedAt: true
      changed_at,
      changed_by,
      total_changed_at
}

